"use client";
/* eslint-disable react-hooks/set-state-in-effect -- authenticated availability is hydrated after mount */
import { CalendarClock, LoaderCircle } from "lucide-react";
import { useCallback, useEffect, useState } from "react";
import type { PlatformLocale } from "../../lib/platform/routing";
type Slot={id:string;specialistType:string;startsAt:string;endsAt:string;timezone:string};
export function ConsultationsClient({locale}:{locale:PlatformLocale}){const ru=locale==="ru";const [slots,setSlots]=useState<Slot[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState("");const load=useCallback(async()=>{try{const response=await fetch("/api/platform/consultations",{cache:"no-store"});const data=await response.json() as {slots?:Slot[];error?:string};if(!response.ok)throw new Error(data.error||"Ошибка");setSlots(data.slots||[]);}catch(value){setError(value instanceof Error?value.message:String(value));}finally{setLoading(false);}},[]);useEffect(()=>{void load();},[load]);async function book(slotId:string){const response=await fetch("/api/platform/consultations",{method:"POST",headers:{"content-type":"application/json","x-juro-csrf":"1"},body:JSON.stringify({slotId})});const data=await response.json() as {error?:string};if(!response.ok){setError(data.error||"Ошибка");return;}await load();}return <section className="consult-workspace"><header><CalendarClock/><div><small>JURO</small><h1>{ru?"Консультации":"Maslahatlar"}</h1><p>{ru?"Выберите свободное время внутреннего календаря. Занятый слот нельзя забронировать повторно.":"Ichki taqvimdan bo‘sh vaqtni tanlang. Band vaqtni qayta bron qilib bo‘lmaydi."}</p></div></header>{error&&<p className="plan-error">{error}</p>}{loading?<LoaderCircle className="spin"/>:<div className="slot-grid">{slots.slice(0,24).map(slot=><button key={slot.id} onClick={()=>void book(slot.id)}><span>{slot.specialistType==="operator"?(ru?"Оператор":"Operator"):(ru?"Старший юрист":"Katta yurist")}</span><strong>{new Intl.DateTimeFormat(ru?"ru-RU":"uz-UZ",{weekday:"short",day:"2-digit",month:"short",hour:"2-digit",minute:"2-digit",timeZone:"Asia/Tashkent"}).format(new Date(slot.startsAt))}</strong><small>Asia/Tashkent</small></button>)}</div>}</section>}
