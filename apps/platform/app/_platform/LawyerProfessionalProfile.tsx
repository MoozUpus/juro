"use client";

import { Save } from "lucide-react";
import { useEffect, useState, type FormEvent } from "react";
import type { PlatformLocale } from "../../lib/platform/routing";

type Profile = { id: string; displayName: string; specialties: string[]; languages: string[]; status: string; publicApprovedAt: string | null; experienceYears: number | null; priceDescription: string | null; availabilityStatus: "unknown" | "available" | "limited" | "unavailable"; nextAvailableAt: string | null; advocateStatus: "not_declared" | "declared" | "verified"; firmName: string | null; bio: string | null };
type Form = { displayName: string; specialties: string; languages: string; experienceYears: string; priceDescription: string; availabilityStatus: Profile["availabilityStatus"]; nextAvailableAt: string; advocateStatus: "not_declared" | "declared"; firmName: string; bio: string };

const blank: Form = { displayName: "", specialties: "", languages: "", experienceYears: "", priceDescription: "", availabilityStatus: "unknown", nextAvailableAt: "", advocateStatus: "not_declared", firmName: "", bio: "" };
const toForm = (profile: Profile): Form => ({ displayName: profile.displayName, specialties: profile.specialties.join(", "), languages: profile.languages.join(", "), experienceYears: profile.experienceYears === null ? "" : String(profile.experienceYears), priceDescription: profile.priceDescription ?? "", availabilityStatus: profile.availabilityStatus, nextAvailableAt: profile.nextAvailableAt ? profile.nextAvailableAt.slice(0, 16) : "", advocateStatus: profile.advocateStatus === "verified" ? "declared" : profile.advocateStatus, firmName: profile.firmName ?? "", bio: profile.bio ?? "" });
const list = (value: string) => [...new Set(value.split(",").map((item) => item.trim()).filter(Boolean))];

export function LawyerProfessionalProfile({ locale }: { locale: PlatformLocale }) {
  const ru = locale === "ru"; const [profile, setProfile] = useState<Profile | null>(null); const [form, setForm] = useState<Form>(blank); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState(""); const [notice, setNotice] = useState("");
  useEffect(() => { fetch("/api/platform/lawyer-profile", { cache: "no-store" }).then(async (response) => { const body = await response.json() as { profile?: Profile | null; error?: string }; if (!response.ok) throw new Error(body.error); return body.profile ?? null; }).then((value) => { setProfile(value); if (value) setForm(toForm(value)); }).catch((value) => setError(value instanceof Error ? value.message : String(value))).finally(() => setLoading(false)); }, []);
  async function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setSaving(true); setError(""); setNotice(""); const payload = { displayName: form.displayName, specialties: list(form.specialties), languages: list(form.languages), experienceYears: form.experienceYears ? Number(form.experienceYears) : null, priceDescription: form.priceDescription || null, availabilityStatus: form.availabilityStatus, nextAvailableAt: form.nextAvailableAt ? new Date(form.nextAvailableAt).toISOString() : null, advocateStatus: form.advocateStatus, firmName: form.firmName || null, bio: form.bio || null, locale }; try { const response = await fetch("/api/platform/lawyer-profile", { method: profile ? "PATCH" : "POST", headers: { "content-type": "application/json", "x-juro-csrf": "1" }, body: JSON.stringify(payload) }); const body = await response.json() as { profile?: Profile; error?: string }; if (!response.ok || !body.profile) throw new Error(body.error || "Ошибка"); setProfile(body.profile); setForm(toForm(body.profile)); setNotice(profile ? (ru ? "Профессиональный профиль обновлён." : "Professional profil yangilandi.") : (ru ? "Профиль создан и ожидает модерации JURO." : "Profil yaratildi va JURO moderatsiyasini kutmoqda.")); } catch (value) { setError(value instanceof Error ? value.message : String(value)); } finally { setSaving(false); } }
  if (loading) return <section className="profile-panels" aria-busy="true"><section><p>{ru ? "Загрузка профессионального профиля…" : "Professional profil yuklanmoqda…"}</p></section></section>;
  return <section className="profile-panels lawyer-professional-profile"><section><h2>{ru ? "Профессиональный профиль юриста" : "Yuristning professional profili"}</h2><p>{ru ? "Сведения публикуются в каталоге только после отдельного одобрения JURO. Статус адвоката «подтверждён» нельзя установить самостоятельно." : "Ma’lumotlar JURO alohida tasdiqlaganidan keyingina katalogda ko‘rinadi. «Tasdiqlangan» advokat maqomini mustaqil belgilab bo‘lmaydi."}</p>{profile && <p>{ru ? `Текущий статус: ${profile.status}` : `Joriy holat: ${profile.status}`}</p>}{error && <p className="profile-message error" role="alert">{error}</p>}{notice && <p className="profile-message success" role="status">{notice}</p>}<form className="profile-form" onSubmit={(event) => void save(event)}><section><label>{ru ? "Отображаемое имя" : "Ko‘rsatiladigan ism"}<input required maxLength={160} value={form.displayName} onChange={(event) => setForm({ ...form, displayName: event.target.value })} /></label><label>{ru ? "Специализации через запятую" : "Mutaxassisliklar, vergul bilan"}<input required value={form.specialties} onChange={(event) => setForm({ ...form, specialties: event.target.value })} /></label><label>{ru ? "Языки через запятую" : "Tillar, vergul bilan"}<input required value={form.languages} onChange={(event) => setForm({ ...form, languages: event.target.value })} /></label><label>{ru ? "Стаж, лет" : "Tajriba, yil"}<input type="number" min="0" max="99" value={form.experienceYears} onChange={(event) => setForm({ ...form, experienceYears: event.target.value })} /></label><label>{ru ? "Фирма" : "Firma"}<input maxLength={180} value={form.firmName} onChange={(event) => setForm({ ...form, firmName: event.target.value })} /></label></section><section><label>{ru ? "Доступность" : "Mavjudlik"}<select value={form.availabilityStatus} onChange={(event) => setForm({ ...form, availabilityStatus: event.target.value as Form["availabilityStatus"] })}><option value="unknown">{ru ? "Не указана" : "Ko‘rsatilmagan"}</option><option value="available">{ru ? "Доступен" : "Mavjud"}</option><option value="limited">{ru ? "Ограниченная" : "Cheklangan"}</option><option value="unavailable">{ru ? "Недоступен" : "Mavjud emas"}</option></select></label><label>{ru ? "Ближайшая доступность" : "Eng yaqin mavjudlik"}<input type="datetime-local" value={form.nextAvailableAt} onChange={(event) => setForm({ ...form, nextAvailableAt: event.target.value })} /></label><label>{ru ? "Статус адвоката" : "Advokat maqomi"}<select value={form.advocateStatus} onChange={(event) => setForm({ ...form, advocateStatus: event.target.value as Form["advocateStatus"] })}><option value="not_declared">{ru ? "Не заявлен" : "Bildirilmagan"}</option><option value="declared">{ru ? "Заявлен, не подтверждён JURO" : "Bildirilgan, JURO tasdiqlamagan"}</option></select></label><label>{ru ? "Описание цены" : "Narx tavsifi"}<input maxLength={280} value={form.priceDescription} onChange={(event) => setForm({ ...form, priceDescription: event.target.value })} /></label><label>{ru ? "О себе" : "O‘zingiz haqingizda"}<textarea maxLength={2000} value={form.bio} onChange={(event) => setForm({ ...form, bio: event.target.value })} /></label></section><button type="submit" disabled={saving || list(form.specialties).length === 0 || list(form.languages).length === 0}>{saving ? null : <Save aria-hidden="true" />}{ru ? "Сохранить профессиональный профиль" : "Professional profilni saqlash"}</button></form></section></section>;
}
