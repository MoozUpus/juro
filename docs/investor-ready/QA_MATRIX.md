# QA matrix

This file is updated with evidence during release. `VERIFIED` means the named environment and mode were actually exercised. `PARTIAL` means a narrower check passed. `NOT TESTED` is intentional and is never upgraded from emulation.

## Browser and device policy

| Target | Status | Scope |
| --- | --- | --- |
| Chrome desktop | PARTIAL | Public, full non-call Client and full non-call Lawyer route suites rendered at desktop sizes with Manrope and no new console warnings. Fresh-MFA Admin overview/routes, fee matrix, the final localized overview polish and the post-`0155` platform audit-log replay passed. Two authenticated Chrome profiles completed device preflight and a two-party TURN-ready WebRTC call. The Client shared only the selected public `juro.uz/ru` tab; Lawyer received the active 1920×962 display stream, and both sides returned to the 1280×720 camera stream after stop. Closing and reopening the Lawyer call tab produced the live `1/3` automatic-reconnect state and recovered the original Client session without reloading it. The authenticated Client dashboard, 19-route Lawyer suite and fresh-MFA Admin data screens also passed real Windows-scale 150% checks |
| Chrome responsive 320/360/375/390/430/768/820/1024/1280/1366/1440/1728/1920 | PARTIAL | The public lawyer catalogue, authenticated Client dashboard and final isolated Admin overview passed every listed width without horizontal overflow. Fresh authenticated loops also passed all 20 top-level Client routes at 390×844 portrait, 768×1024 tablet and 844×390 landscape: canonical final URL, Manrope, no 404, no page overflow and no warning/error browser log. In landscape, the AI composer remained reachable above the fixed mobile navigation without overlap. Below the wide breakpoints, the observed content width can be the requested width minus the 15-pixel vertical scrollbar. Representative Lawyer dashboard/monitoring/AI/billing/profile coverage passed at 360/390/768/1366/1440; full all-route coverage at every width is not claimed |
| Chrome zoom 125% / 200% | NOT TESTED | Explicitly excluded by the user; native page zoom must not be changed |
| Windows scale 125% | VERIFIED | Live Chrome reported `devicePixelRatio=1.25`; this is host evidence, not a browser-zoom substitute |
| Windows scale 150% | VERIFIED | Settings was changed from 125% to the recommended 150%. The authenticated Client dashboard reported DPR 1.5, a 1280×665 viewport and no horizontal overflow. A resumed production pass exercised 19 authenticated Lawyer routes at DPR 1.5 and a 1536×769 viewport; every root width stayed within the viewport. A fresh-MFA Admin session then rendered the isolated overview, profiles, reviews and Legal Corpus plus the Platform fee matrix, audit log and lawyer lifecycle screen at DPR 1.5 and 1280×609. Every root stayed within the viewport; Manrope was loaded, and the screens exposed the expected synthetic deletion, 1%/2%/5% sandbox billing and immutable audit data. The host was restored to DPR 1.25; native Chrome page zoom was never changed |
| Reduced motion | VERIFIED | Windows Animation effects were changed from on to off and restored to on. The authenticated Chrome dashboard changed `prefers-reduced-motion` from false to true and back to false; `body` transition duration reduced from 180 ms to 0.01 ms and the Platform sidebar from 220 ms to the intentional 120 ms bounded feedback, then both returned to their original values |
| Edge | NOT TESTED | Explicitly excluded by the user |
| Firefox | NOT TESTED | Explicitly excluded by the user |
| Safari/WebKit | NOT TESTED | Explicitly excluded by the user |
| Physical iPhone/iPad | NOT TESTED | Explicitly excluded by the user |
| Physical Android | NOT TESTED | Explicitly excluded by the user |

## Role flows

| Role | Flow | Status |
| --- | --- | --- |
| Guest | Public site, public lawyer directory/profile, auth redirects | VERIFIED — production HTTP/redirect plus Chrome home, catalogue, demo profile and trust surfaces; Light/Dark/System and representative responsive widths passed. The fresh public-edge sweep resolved all six named hosts, preserved the `www` 308 canonical redirect and returned HTTP 200 for all 78 canonical RU/UZ/EN sitemap URLs |
| Client | Dashboard, AI, documents, marketplace, request, chat, consultation, demo billing | PARTIAL — production replay covered every named non-call surface, exact case/access-grant/messages/consultation state, source-linked task notification, themes, responsive dashboard widths and foreign Lawyer-route denial. A privacy-safe production screenshot retains the existing Client↔Lawyer message history using only records marked `SYNTHETIC DEMO`. Client Demo completed device preflight, a live two-party call, selected-source public-tab screen sharing, a forced Lawyer-tab reconnect and a terminal grounded AI answer whose direct official Lex.uz source was opened in Chrome. After Worker 123, a Lawyer-only end action also propagated to Client through the bounded heartbeat, disabled the end control and reduced both Client media elements to `readyState=0`, 0×0 without a Client click. After migration `0156` and Worker `00e80afc-a659-4158-827b-1b73228cf862`, authenticated Chrome loaded the two-message request history, filtered it by search, opened/cancelled a reply preview, rendered 1521/1521 without overflow, and exposed neither private AI nor internal notes to Client. The later confirmed synthetic replay proved Client send, Lawyer reply, both typing indicators and read/delivery state; it reconfirmed that the Lawyer AI summary and private note were absent from Client. Exact mutable replay rows were removed and the original seed state was restored. A fresh post-`0157` protected-route replay on 23 August enabled the real ASUS FHD webcam and Realtek microphone, rendered camera/microphone/speaker selectors, switched and restored the microphone, switched the speaker, and exposed the localized network-quality state. Media was released by leaving the room; no end/no-show mutation was submitted |
| Lawyer | Dedicated host, dashboard, request/client/matter access, tasks, knowledge, call, billing, profile | PARTIAL — all non-call routes, Light/Dark/System, five responsive widths and a real Windows-scale 150% 19-route sweep were verified in authenticated Chrome; timer, conflict, knowledge and simulated-payment records were confirmed in production D1. The 150% sweep exposed clean-path hydration falling back to Requests for record views. Commit `06239de4` added pathname-aware RU/UZ hydration, CI run `32599975102` passed, Worker 121 deployed it, and authenticated production Chrome then rendered the correct Requests, Schedule, Clients, Matters, Messages, Documents and Tasks screens on their visible clean URLs. Lawyer Demo completed device preflight, two-party media, selected-source screen reception, forced tab-close/reopen recovery and clean end-call propagation. Production D1 recorded `reconnected`, the replacement Lawyer `joined`, one terminal `ended`, both participant `left_at` values and zero remaining signals. The `0156` chat release now renders search, reply/copy/pin controls, typing/read/delivery foundations, eight private AI-assist actions and private document-linked notes convertible to a task. Chrome exposed an invalid `user_profiles.display_name` query on the lawyer-only path; commit `e25a9fee` switched both reads to `lawyer_profiles`, CI `32609779920` passed, and the hotfix Worker then loaded the same synthetic history and both private panels at 1521/1521 without overflow. After `0157`, authenticated RU and UZ Lawyer Chrome rendered two proposal/consultation context cards, the call action and the private AI/note boundaries at 1521/1521 without overflow. The exact confirmed write replay then passed reply linkage, typing/read/delivery, pin/unpin, one private OpenAI summary and one private note→task conversion; Client saw neither private surface, mutable replay rows were removed, six workspace audit events and the completed AI run remained, and D1 integrity stayed clean. Protected call navigation was browser-blocked, so the deployed asset check for device switching/network quality is not claimed as a live selector pass |
| Admin | Host handoff, lawyer controls, deletion request, fees, transaction/audit | VERIFIED — TOTP enrollment and fresh-MFA host handoff passed; isolated overview/lawyers/reviews/legal-corpus plus fee matrix, sandbox transactions and configuration audit passed. The D1 compound-query P1 and hash-constraint fault were fixed, and the final fresh-MFA Chrome audit-log replay plus immutable access events passed. After commit `4751d3c7` corrected the administrator profile capability and expired-session UX, fresh admin-only MFA opened the RU and UZ profile controls directly and after refresh. Chrome rendered two published profiles, the active 90-day trial, the pending synthetic deletion request and independent trust designations with no console/network error, overflow or submitted mutation. Commit `06239de4` generalized the safe expired-session surface to all enabled legacy Admin pages: post-deploy Chrome verified 13 RU routes plus an UZ representative without `Not Found` or overflow; `/legal-sources/reviews` remains an intentional 404 while its production feature flag is disabled |
| Cross-role | Foreign client, document, billing and admin route denial | PARTIAL — automated backend tests plus production Client denial of Lawyer, Admin and document data. A fresh owner-accessible individual registration completed through production email OTP and onboarding, then protected RU and UZ Lawyer routes reached localized role boundaries without a loop. Both pages identified the Client role, stated that current Client data was not disclosed, rendered Manrope at 1536/1536 without overflow, exposed no Lawyer-private labels and provided a 44-pixel return link to the matching Client locale. Admin console and billing routes reached protected re-auth without disclosing staff or fee data. An existing synthetic Lawyer document exposed none of its Lawyer Demo/counterparty text, but the denied page still rendered an empty receipt editor; commit `1c40b64b`, focused 57/57, full core 1071/1071 and CI `32644074770` now enforce `loadStoredDocument` followed by `notFound()` before editor render. Worker `0234c879-eba3-49d4-81bd-9759b77ffcfd` deployed the fix. A post-deploy synthetic owner document opened successfully after full reload and was then fully cleaned with an empty foreign-key check. Chrome blocked direct automated `/documents/:id` navigation and the stale deleted-ID tab could not be reclaimed for the final visual 404, so this row remains PARTIAL rather than inferring that post-deploy foreign-ID screen |

## Theme matrix

| Surface | Light | Dark | System |
| --- | --- | --- | --- |
| `juro.uz` | VERIFIED | VERIFIED | VERIFIED |
| `app.juro.uz` auth/dashboard | VERIFIED | VERIFIED | VERIFIED |
| `lawyer.juro.uz` dashboard/workspace | VERIFIED | VERIFIED | VERIFIED |
| Client documents/calendar/marketplace/profile and remaining top-level modules | VERIFIED — all 20 top-level routes passed at 390×844 with the light shell and dark text computed on every route, with no 404, overflow or warning/error log | VERIFIED — the same 20-route 390×844 loop retained the dark shell and light text computed on every route, with no 404, overflow or warning/error log | VERIFIED — System was restored, survived a reload with its control still pressed and returned to the normal 1536-pixel dashboard viewport |
| Admin | VERIFIED — fresh-MFA production Chrome selected Light at 1536 pixels with semantic light colors, Manrope and no overflow | VERIFIED — fresh-MFA production Chrome selected Dark, measured semantic dark colors, Manrope and no overflow, and retained the mode after reload | VERIFIED — System was restored, remained pressed after reload and followed the OS light preference without overflow |

Automated tests enforce shared-cookie precedence over stale per-host storage,
cross-host scope, all three Admin controls, nonce-bound script CSP and 44-pixel
targets. Fresh-MFA production Chrome verified Light, Dark and System at 1536
pixels without overflow on Admin Worker
`67065fd8-fcc8-4c15-93c8-bc7b46ce4fcb`. Dark and the finally restored System
mode each survived a reload.

## Release gates

| Gate | Status |
| --- | --- |
| Monorepo lint/type-check | VERIFIED locally |
| Website build/tests | VERIFIED locally |
| Platform focused regression tests | VERIFIED locally — current Lawyer decision, entry and host-routing suites passed 23/23, including concurrent decision fencing, clean document-builder deep links and known nested compatibility aliases |
| Full platform suite | VERIFIED — release code head `7b36fc25` passed rendered HTML 33/33, core 1074/1074 and Cloudflare 201/201. GitHub CI run `32694042835` passed Website in 55s and Platform in 7m57s, including lint, type-check, tests, deployable artifact, Cloudflare environment matrix, production dependency audit and licence policy |
| Production artifact | VERIFIED locally and in CI — production build/bindings/artifact, performance budgets and release-script dry-run passed before deployment |
| Approved RU/UZ policies | VERIFIED — all ten approved pages, exact digests, append-only production registry rows, HTTP and Chrome rendering passed. A fresh owner-accessible individual registration completed through production email OTP at `2026-08-23T13:17:43.295Z`. Read-only D1 evidence found exactly three RU `registration_checkbox` acceptances authenticated by `email_otp` for Terms, Privacy and Personal Data version `2026-08-23.1`; every acceptance SHA matched its approved policy row, evidence source was `registration`, active marketing consent count was zero and the live foreign-key check was empty |
| D1 backup/restore and migrations | VERIFIED — migrations through `0158_lawyer_request_decisions.sql` are applied with no pending migration. The `0158` 123,073,542-byte pre export (`35e2aaa8eac0ec9ee3b8e36004e95d7328d7f7f265fc3395bab938f97b876d4c`) and 123,075,692-byte post export (`1ed29b9ca924b2ccd6244fddfad770eec975bbd2d8809850183073666a3fed0d`) plus manifests are in private R2 under `d1/juro-production/20260823T232118Z-0158-f4941ff9/` with exact byte/SHA-256 readback. Both isolated restores returned `quickCheck=ok` and zero FK violations; post restore reports 157 migrations, 281 tables, 607 indexes and 378 triggers. Live verification found all three decision-evidence columns, the unique partial claim index and an empty `foreign_key_check`. The exact local plaintext/SQLite/readback directory was permanently removed after the verified private backup round trip; `Test-Path` returned false. The earlier `0156`/`0157` recovery evidence remains recorded in the production evidence file |
| Cloudflare Calls TURN secret | VERIFIED — production TURN key rotated, both Worker secrets rebound without printing or persisting their values, and both participants received `relayAvailable=true`; the production room recorded `provider=cloudflare_realtime_turn` |
| Production deployment | VERIFIED — final Platform Worker `927f5a61-1bf0-4d14-9a7e-cf6b0cbdc1a5` receives 100% traffic; immediate rollback version is `4ce7db88-f20f-49ba-b887-b3496b02451c`. Isolated Admin Worker `67065fd8-fcc8-4c15-93c8-bc7b46ce4fcb` receives 100% traffic with `9cdbf8b1-ae02-4c32-9941-85d593064038` as rollback; public Worker `b87fb3e5-65f3-45ea-9d73-c4ff31d57116` and Sites 74 remain live |
| `/api/status` after deployment | VERIFIED — app/status-host reads after Worker `927f5a61-1bf0-4d14-9a7e-cf6b0cbdc1a5` returned `overallStatus=operational`, exact components `platform`, `otp`, `ai`, `document_analysis`, `upload`, `document_builder`, `email`, `lawyer_area` all operational and zero active/recent incidents; the latest recorded response was generated at `2026-08-24T05:41:28.853Z` |
| Authenticated Chrome QA | PARTIAL — Client and Lawyer non-call desktop suites complete; the Client dashboard and latest Admin overview passed all 13 requested widths, while every top-level Client route passed fresh 390px mobile and 768px tablet loops and representative Lawyer widths passed; live grounded Client AI and direct Lex.uz source passed. Camera/microphone preflight, synchronized two-party WebRTC, selected-source public-tab screen sharing, explicit share stop, live Lawyer-tab forced reconnect, mute/camera controls and remote end propagation all passed in two separate authenticated Chrome profiles. The forced reconnect exposed attempt `1/3`, then recovered the original Client timer and both remote media streams after the Lawyer tab rejoined. After Worker 123, a Lawyer-only end reached Client on the next bounded heartbeat; both sides exposed 0×0 stopped media and D1 recorded both `left_at` values with zero signals. Real Windows-scale 150% evidence covers the Client dashboard, 19 authenticated Lawyer routes and fresh-MFA Admin data screens; Windows was restored to 125%. The post-`0155` fresh-MFA audit-log replay and RU/UZ trial/deletion controls passed. After `0156`, two authenticated profiles verified role-private chat rendering, search, reply preview, Manrope and 1521/1521 containment; Chrome also found and verified the `e25a9fee` lawyer-only SQL hotfix. The exact later synthetic write replay passed send/reply, both typing indicators, delivery/read, pin/unpin, one private AI summary and one private note→task conversion; privacy boundaries and cleanup were verified in Chrome and D1. After approved-policy Worker `c90f5dd6-459c-4358-9ccd-3316a45e6aab`, all ten RU/UZ legal pages rendered with their approved label, exact digest and no overflow. The fresh protected Client call-route replay also passed real camera/microphone/speaker enumeration and selection. A new owner-entered OTP plus TOTP session opened the isolated Admin console and verified System/Light/Dark, semantic colors, reload persistence, Manrope and 1536/1536 containment; System was restored. A separate fresh production Client registration then completed through email OTP and onboarding; Worker `0b145b96-a45e-465b-b8a0-779d692dae93` rendered the localized RU and UZ Lawyer-role boundary with correct return locale, 44-pixel targets, Manrope, 1536/1536 containment and no Lawyer-private labels. Connected production Chrome now proves that the previously broken `/ru/lawyer/requests` compatibility URL reaches localized Lawyer authentication with the exact return path. The new `0158` accept/request-information/decline controls and exact conflict-clear→access-grant→accept transition are not yet claimed as authenticated live Chrome evidence because OTP dispatch remains intentionally paused for its required action-time confirmation. Native page zoom remains intentionally `NOT TESTED` by user instruction |
