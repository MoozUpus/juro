# Production evidence — 2026-08-22

This is a current-state release record, not a blanket completion claim. Browser and device gaps remain governed by `QA_MATRIX.md`.

## Database and synthetic demo data

- Production D1 migrations `0146` through `0154` were applied sequentially. Corrective migration `0155_platform_audit_hash_constraints.sql` was later applied after Chrome exposed a D1-incompatible expanded `GLOB`; a fresh production migration list again reported no remaining migrations.
- Pre- and post-migration exports were restored locally. The latest post-migration restore returned `quickCheck=ok` and zero foreign-key violations. The live read-only schema inventory reports 154 migrations, 280 tables, 601 non-system indexes and 371 triggers, ending at `0155_platform_audit_hash_constraints.sql`.
- Private R2 upload/readback SHA-256 checks matched for both exports. Verified local plaintext SQL/SQLite/readback copies were then removed.
- Migration `0155` received its own full pre/post recovery gate. The 89,558,457-byte pre export (`FF582B04B4612A8DC0E119763E59B3AFF13EE69D508AB50C267420D9E9D1C741`) and 89,562,342-byte post export (`805E2BFF06A20A787E68A5EF8F3C590E041FB2FF6A9FB37BCF49B65824214167`) plus manifests were read back exactly from private `juro-production-backups`. Both isolated restores returned `quickCheck=ok` and zero foreign-key violations; the live remote `foreign_key_check` was empty. Remote `quick_check` returned `SQLITE_NOMEM`, so only the isolated restore is claimed for that check. All ten verified local plaintext/SQLite/readback artifacts were removed after the private backup round trip.
- Bounded production checks returned exactly three active demo-account registry rows and one consent-published demo lawyer. A later Chrome rehearsal created one additional, explicitly simulated payment run; it is recorded below rather than represented as a real transaction.

## Realtime and deployment

- Cloudflare Realtime was activated. The initial `juro-production-webrtc` key was diagnosed as unusable by the call credential exchange, so a rotated production key named `juro-prod-turn-v2-20260822` was created.
- `CLOUDFLARE_TURN_KEY_ID` and `CLOUDFLARE_TURN_KEY_API_TOKEN` were atomically rebound to Worker `juro`. Values were never printed, written to disk or committed. Both participant preflight responses then returned `relayAvailable=true`; the production room recorded `provider=cloudflare_realtime_turn`.
- Platform Worker production version: `e8fc00ed-6249-4e04-9300-8732a4a05e91`.
- Isolated admin Worker production version: `9cdbf8b1-ae02-4c32-9941-85d593064038`.
- Public routing Worker production version: `b87fb3e5-65f3-45ea-9d73-c4ff31d57116`.
- Sites version 74 is live from commit `55cf9788818a14c7fd6a8207500e3df59889cc71`. It retains the canonical unlocalized lawyer catalogue/profile redirects and presents consent-published profiles with the neutral `Новый профиль` designation rather than implying JURO approval.

## Production URL smoke

| URL | Evidence |
| --- | --- |
| `https://juro.uz/` | 200 |
| `https://www.juro.uz/` | 308 to canonical `juro.uz`; path and query are retained |
| `https://juro.uz/lawyers` | 308 to `/ru/lawyers`, then 200 |
| `https://juro.uz/lawyers/{profileId}` | Sites 74: 308 to `/ru/lawyers/{profileId}` with the query string retained, then 200 |
| `https://app.juro.uz/` | 307 to localized authentication for an unauthenticated request |
| `https://lawyer.juro.uz/` | 200; protected professional routes enforce authentication |
| `https://admin.juro.uz/` | 303 to the isolated app admin handoff surface |
| `https://status.juro.uz/` | 200 |
| `https://staging.app.juro.uz/` | 302 to Cloudflare Access |
| `https://admin.staging.juro.uz/` | 302 to Cloudflare Access |
| `https://status.staging.juro.uz/` | 200 |

Repository-only or inactive names `staging.juro.uz`, `app.staging.juro.uz`, `lawyer.staging.juro.uz`, `api.juro.uz` and `local.juro.uz` have no live A/AAAA answer and are not represented as production surfaces.

## Health

At `2026-08-22T10:42:32.147Z`, `/api/status` returned `overallStatus=operational`, all eight published components operational and no active incidents. A request made during the immediate post-deploy probe refresh briefly observed stale evidence; the subsequent authoritative response was operational after the scheduled/synthetic probes completed.

A later read at `2026-08-22T11:01:37.131Z` again returned `overallStatus=operational`, all eight components operational and no incidents. The public sitemap smoke followed all 78 canonical URLs without a 4xx/5xx response.

After the monitoring-task, lawyer-layout, Turnstile-locale and audit-query deployments, a fresh read at `2026-08-22T14:14:22.408Z` remained `overallStatus=operational`, with all eight components operational and no active incidents.

After the final official-URL hardening and Admin localization deployment, a fresh read at `2026-08-22T14:37:45.993Z` again returned `overallStatus=operational`, all eight components operational and zero active incidents.

After the call security-policy, TURN rotation, end-call and final contrast fixes, a fresh read at `2026-08-22T15:46:44.553Z` returned `overallStatus=operational`, all eight components operational and zero active incidents.

The immediate read after Worker `727eacbe-7fb6-4012-87a9-3e290edd525b`, generated at `2026-08-22T16:36:51.937Z`, reported no incidents and operational D1, queues, DLQ, OpenAI, Anthropic, malware-scan and lawyer-area evidence. Its aggregate status was nevertheless `stale` because the private-R2 probe was 671 seconds old. This record is not promoted to a healthy post-release gate until a fresh probe returns the aggregate to `operational`.

The next scheduled production probe generated `2026-08-22T16:40:54.780Z` and closed that freshness gap: `overallStatus=operational`, all eight components operational, no stale dependencies and zero active incidents.

After production migration `0155`, a fresh read generated at `2026-08-22T17:20:15.054Z` remained `overallStatus=operational`, all eight components operational, no stale/degraded component and no active incident.

A later release-gate read generated at `2026-08-22T17:40:37.529Z` again returned `overallStatus=operational`, all eight components operational, no stale/degraded dependency and no active or recent incident.

The final pre-commit health read generated at `2026-08-22T17:59:06.166Z` remained `overallStatus=operational`: 8/8 components operational, zero stale/degraded component and zero incident.

After the administrator lawyer-profile access fix was deployed as Worker `e8fc00ed-6249-4e04-9300-8732a4a05e91`, independent reads through both `app.juro.uz/api/status` and `status.juro.uz/api/status` generated at `2026-08-22T18:20:55.816Z` and `2026-08-22T18:20:57.096Z`. Both returned `overallStatus=operational`, all eight components operational and no active or recent incident.

## Authenticated Chrome evidence

- Public Chrome evidence covered the RU home, catalogue, consent-published demo
  profile and Trust Center. Light, Dark and System modes and 360/390/768/1366/1440
  representative widths passed without page overflow or console warnings.
- Lawyer production routes verified without console warnings or page overflow: dashboard; requests, schedule, matters, clients, messages, documents and tasks; profile; calendar; security; billing; AI chat; document builder/review; monitoring; knowledge; settings; demo payments; and help.
- Lawyer Light, Dark and System modes switched visibly and returned the matching `aria-pressed` state. System was restored after the check.
- Chrome responsive evidence passed at 360, 390, 768, 1366 and 1440 pixels for representative lawyer dashboard, monitoring, AI, billing and profile surfaces.
- The live Lex.uz feed exposed a two-level intrinsic grid-width defect. Commits `49ceed62` and `9dc062fa` constrained the feed and cards; the post-deploy desktop measurement changed from 3013 pixels of document width to 1521/1536, while the 390-pixel viewport measured 375/390 with no overflow.
- The deployed monitoring action created a real tenant-scoped task from one live Lex.uz metadata event, retained the exact official source and exact case, and produced a client notification linking to that task. Both Client and Lawyer production Chrome views rendered the source safely.
- The Client production route replay covered dashboard, saved clarification-first AI history, populated synthetic document/preview, lawyer marketplace/profile, accepted request, active access grant, messages, confirmed consultation, case plan, calendar, billing/demo payments, notifications, profile, settings, security and monitoring. The desktop pages used Manrope, showed no page overflow, and the client was denied a foreign lawyer route without data exposure. Light, Dark and System theme controls passed and System was restored.
- The authenticated Client dashboard also passed 360, 390, 768, 1366 and 1440-pixel Chrome widths without page overflow. The initial 360-pixel capture caught the mobile drawer during its intended exit transition; the post-transition measurement and replacement capture confirmed the drawer fully off-canvas and the document width contained.
- A later clean Client-profile replay extended that dashboard evidence to the full requested list: 320, 360, 375, 390, 430, 768, 820, 1024, 1280, 1366, 1440, 1728 and 1920 pixels. Every measurement retained Manrope and matched root/body scroll width to content width with no horizontal overflow. The public lawyer catalogue passed the same 13-width loop independently.
- A fresh authenticated Client responsive replay then exercised 20 top-level routes at both 390×844 and 768×1024: dashboard, AI chat, document builder/review, cases, documents, action plans, calendar, archive, history, consultations, lawyer marketplace, monitoring, notifications, billing, profile, settings, security, demo payments and help. Every final URL remained canonical, every page used Manrope, no route rendered a 404, root scroll width stayed within client width and the Chrome warning/error log was empty. The temporary responsive viewport override was reset and the dashboard returned to the normal 1536-pixel window afterward; this is not physical-device evidence.
- The same 20-route Client set passed a later 844×390 landscape loop under restored System with no page overflow, 404 or warning/error log. The loaded AI-chat composer was then scrolled into view and measured separately: its bottom was 221 pixels while the fixed mobile navigation began at 322 pixels, so the input remained reachable without overlap. The viewport was reset to the normal 1536-pixel dashboard. This is landscape responsive evidence, not a physical mobile-keyboard claim.
- A follow-up theme replay exercised the same 20 Client routes at 390×844 in explicit Dark and again in explicit Light. Every route retained the selected `data-theme`, canonical URL and Manrope, with no 404, root overflow or browser warning/error log. Computed shell/text colors changed from `rgb(9,20,30)` / `rgb(238,243,246)` in Dark to `rgb(248,246,242)` / `rgb(16,35,51)` in Light, proving that more than the body background changed. System was restored, survived a reload with its control still pressed and remained selected after the responsive override was reset to the normal 1536-pixel dashboard.
- A live Client AI request completed in about 12 seconds with the answer “21 calendar days” and cited article 217 of the Labour Code. Its direct `https://lex.uz/ru/docs/6257291` link opened in a second authenticated Chrome profile; the official page contained both article 217 and the stated 21-day minimum. A broader query that could not be grounded failed closed, charged no quota and its synthetic conversation was deleted rather than retained as demo history.
- The Lawyer rehearsal started and stopped a five-second billable timer, ran a one-result conflict check with the human-review warning, created a case-linked favourite knowledge note, and executed an isolated demo payment through `previewed → succeeded → refunded`. Read-only production D1 checks confirmed all four records; the payment remained `provider=demo`, `is_simulation=1`.
- A fresh UZ Lawyer login after Worker `fb5607d6-679b-46b6-92c0-e92c612dd240` produced an empty console log. The client now maps RU to Turnstile `ru` and UZ to `auto`, avoiding the unsupported-language fallback warning without weakening server verification.
- Admin Demo enrolled a new TOTP factor. The first diagnostic enrollment was replaced and is `disabled`; the second is `active`, with no failed verification attempts. A fresh-MFA handoff then created a separate, 15-minute host-only production Admin session.
- The fresh Admin session verified `admin.juro.uz` overview, lawyer profiles, review moderation and Legal Corpus in Chrome with Manrope and no desktop overflow. D1 append-only evidence records the issued/consumed handoff and each route view. The fresh-MFA fee matrix also loaded 1%, active 2%/5% rules, sandbox-only transactions and immutable configuration history with 1521/1521 layout width. The final overview polish then rendered its localized publication semantics, four evidence-rich KPI cards, access boundary and quick links. Its overview passed 320, 360, 375, 390, 430, 768, 820, 1024, 1280, 1366, 1440, 1728 and 1920-pixel Chrome viewport checks without horizontal overflow.
- A bounded production D1 aggregate confirmed that the investor dataset still contains one active 90-day lawyer trial, one pending synthetic profile-deletion request and 12 records explicitly constrained to `provider=demo` and `is_simulation=1`. Chrome then exposed the real access defect behind the earlier ambiguous navigation failure: the latest signed-in operator had the `administrator` role, but the role matrix reserved `lawyer.profiles.moderate` for `legal_reviewer`; an expired 15-minute MFA window was also rendered as an opaque 404. Commit `4751d3c7` grants the administrator the bounded profile capability while preserving server-side fresh-MFA enforcement, and replaces the opaque expired-session response with the protected reauthentication surface returning to the exact route. Worker `e8fc00ed-6249-4e04-9300-8732a4a05e91` deployed that fix. A fresh admin-only MFA session then opened the RU and UZ routes directly and after refresh, rendered two published profiles, the active 90-day trial, the pending synthetic deletion request and independent trust-designation controls. The trial, deletion, lifecycle and trust mutations remained disabled without their required reason; no decision or state change was submitted. Both locales had empty console/network logs and a 1521/1521 document width.
- Chrome found a production audit-log P1: D1 rejected the previous seven-term compound SELECT. The fix first shipped in Worker `073aac71-2aa2-4083-948e-1c4c12f1fd68` and is retained in current Worker `e8fc00ed-6249-4e04-9300-8732a4a05e91`: bounded allowlisted per-source queries plus a safe top-N merge. Focused tests and all seven production D1 source queries succeeded read-only. A later fresh-MFA replay reached the POST route but returned `PLATFORM_AUDIT_ACCESS_WRITE_FAILED`; read-only production checks proved role, TOTP, session, device and assignment state valid, then reproduced D1's `LIKE or GLOB pattern too complex` failure from the expanded 64-term hash constraint. Migration `0155` replaced all four constraints with D1-safe length and bounded character-class checks while preserving the immutable table, indexes and guards. New fresh-MFA Chrome loads then rendered the localized audit table without console warnings/errors and confirmed immutable-chain receipts. A read-only production D1 aggregate verified two access events: the first at `2026-08-22T17:45:54.908Z` and the reload at `2026-08-22T17:50:00.687Z`; no actor, session or hash value was copied into this report.
- Initial call preflight exposed a production `Permissions-Policy` defect: camera was globally disabled even after Chrome site permission was allowed. Current Worker `e8fc00ed-6249-4e04-9300-8732a4a05e91` retains the route-scoped fix: camera, microphone and display capture are permitted only on exact protected consultation-call routes. The room also localizes device/API errors and treats simultaneous participant end requests idempotently.
- Client Demo and Lawyer Demo, authenticated in two separate Chrome profiles, both passed camera/microphone preflight. They joined the same production room, showed matching timers, exercised mute/unmute and camera controls, enabled the screen-share control, and ended simultaneously with no raw `CALL_ENDED` code. D1 recorded two prepared events, two joined events, one room end and `provider=cloudflare_realtime_turn`; immutable workspace audit evidence remains after the bounded demo-room reset.
- A follow-up two-profile room launched Chrome's native screen-share picker from the Client control, proving the browser handoff. No source was selected, so actual capture and remote rendering remain `NOT TESTED`; forced reconnect remains `NOT TESTED`. Both tabs were navigated away to release devices, and the exact ended room `76e84656-7d32-4f99-a979-0ec3dd5cda59` was removed with its cascaded ephemeral events while immutable audit evidence remained.
- A 2026-08-23 (Tashkent) call-lifecycle audit found two defects that the earlier successful room did not exercise: reconnect exposed only a manual control, and a display-capture stream could survive a failed track replacement, reconnect, end-call or component teardown. Commit `6eaad19d` now performs at most three automatic reconnect attempts, obtains fresh short-lived TURN credentials through the existing protected prepare action, creates an ICE-restart offer on a fresh peer, ignores callbacks from superseded peers and retains a localized manual fallback. Screen sharing now has an explicit stop control and fail-closed cleanup on every exit path. Focused call tests passed 6/6; the full CI run `32592751302` passed Website and Platform, including rendered HTML 33/33, core 1065/1065 and Cloudflare 201/201. Production Worker version `b9481033-bd24-4dda-8f75-61b8a7ce2473` (version 119) deployed the change. Its hashed `LawyerCallRoom` asset returned HTTP 200 and contained the bounded reconnect and stop-sharing UI. This proves source, artifact and deployment, not a live network interruption: selected-source remote rendering and forced reconnect remain `NOT TESTED`.
- A fresh header audit then found that non-call Platform routes still advertised `microphone=(self)`. Commit `b9faf4bd` scopes microphone access to the same exact protected call-route allowlist as camera and display capture; all other routes now emit `microphone=()`. Local type-check, lint, production build, artifact validation and rendered HTML 33/33 passed, and GitHub CI run `32594218794` passed both Website and Platform. Production Worker `2ab9e425-6a1f-4126-a244-99f27f25861c` (version 120) deployed the fix. Fresh HTTP reads of the app login, Lawyer root and status root all returned HTTP 200 with `camera=(), display-capture=(), geolocation=(), payment=(), usb=(), microphone=()`. The protected call-route allow branch remains covered by rendered tests; this post-deploy audit does not claim a new live call preflight.
- Windows display scale was temporarily changed through Settings from 125% to the recommended 150%, then restored to 125%. At 150%, the authenticated Client dashboard reported `devicePixelRatio=1.5`, a 1280×665 viewport and a 1265-pixel document width with no horizontal overflow; the live screenshot also showed the navigation, header, primary prompt and Today panel without clipping. The Chrome extension channel disconnected after the scale transition: one bounded reconnect listed the two QA tabs, but the next Lawyer navigation failed again. Therefore the Client 150% check is recorded as partial evidence, Lawyer/Admin 150% coverage is not inferred, and the final Settings screenshot confirms restoration to 125%.
- A stable follow-up on 2026-08-23 repeated the real Windows transition and exercised 19 authenticated production Lawyer routes at `devicePixelRatio=1.5` and a 1536×769 viewport. Every document root stayed at or below the viewport width; the live dashboard screenshot showed navigation, header, trial state, publication state and KPIs without clipping. The same run rendered the protected Admin re-auth surface and isolated-host handoff at DPR 1.5 with a 1280×609 viewport and no root overflow, but did not infer fresh-MFA Admin data-screen coverage. Chrome also exposed a functional clean-route defect: the external `clients`, `matters`, `messages`, `documents` and `tasks` paths hydrated back to the default Requests view after the Worker rewrite hid its internal query. Commit `06239de4` resolves the view from the visible RU/UZ pathname and adds focused hydration coverage. Settings was visibly restored to 125% and native Chrome page zoom was never changed.
- GitHub CI run `32599975102` passed Website and Platform at commit `06239de4` (including rendered HTML 33/33, core 1066/1066 and Cloudflare 201/201). The reviewed production release script passed its dry-run and deployed platform Worker `5438d2e3-b0b5-4e4e-8af6-c1fc910aabcd` (version 121) at 100% to the app, Lawyer, Admin and status hosts. Fresh authenticated Chrome then rendered Requests, Schedule, Clients, Matters, Messages, Documents and Tasks on their exact clean Lawyer URLs at DPR 1.25, with 0.48–1.43 second navigation times and no root overflow. The Clients view completed its data load and truthfully showed no real records. Thirteen enabled RU legacy Admin pages plus the UZ `ai-settings` representative rendered the localized protected re-auth surface instead of `Not Found`, also without overflow. `/ru/admin/legal-sources/reviews` remains an intentional 404 because `LEGAL_SOURCE_STAFF_API_ENABLED=false` is a production feature gate evaluated before staff access. Independent app/status API reads generated at `2026-08-22T21:47:14.564Z` and `2026-08-22T21:47:15.633Z` returned `overallStatus=operational`, all eight components operational and zero active or recent incidents.
- A final typography audit found one remaining inline `Inter` declaration on the protected Admin handoff launch screen. Commit `9081ae7e` changed that surface to `Manrope` and added a regression assertion that rejects a restored `Inter` declaration. Focused platform-core 77/77, lint, type-check, the full Platform suite and the production artifact passed locally. GitHub CI run `32602159007` then passed Website in 58 seconds and Platform in 6 minutes 14 seconds. The reviewed production release script passed its dry-run and deployed Worker `c03c3516-4828-44ab-bac6-74b85f45c7b4` (version 122) at 100% to the app, Lawyer, Admin and status hosts. The live content-hashed `AdminConsoleLaunch` asset returned HTTP 200, contained `Manrope` and no longer contained the previous `Inter, ui-sans-serif` declaration. Independent app/status reads generated at `2026-08-22T22:33:15.747Z` and `2026-08-22T22:33:17.064Z` returned `overallStatus=operational`, all eight components operational and no incident object.
- Client Demo and Lawyer Demo were reauthenticated in two separate Chrome profiles on 2026-08-23 (Tashkent), completed camera/microphone preflight and established a live production P2P connection with synchronized timers for more than three minutes. Chrome's native source picker was opened twice from the Client share control, including once with the call tab explicitly foregrounded, but the selected-source capture never became active: the control remained `Показать экран`, and the remote side continued to expose only the ordinary two live video elements. No screen-share or reconnect pass is inferred. Both tabs were navigated away, each reported zero remaining video elements, and the exact synthetic room `63291489-0226-4469-8949-21578d95de04` was removed with its 2 presence rows, 29 ephemeral signals and 4 room events; a post-delete aggregate returned zero for the room and every cascaded table. Audio/video were never recorded. A privacy-safe screenshot of the existing `SYNTHETIC DEMO` Client↔Lawyer message history was retained as `screenshots/after/client-lawyer-synthetic-chat-1536.png`; it contains no real contact data or camera frame.
- A fresh Admin reauthentication then created the isolated 15-minute host-only session at real Windows scale 150%. Chrome rendered the isolated overview, profiles, reviews and Legal Corpus plus the Platform fee matrix, audit log and lawyer lifecycle screen at `devicePixelRatio=1.5` and a 1280×609 viewport. Every route stayed within the viewport, avoided login/404 fallback and loaded Manrope. The data views exposed the fixed 1% policy, active 2%/5% rules, sandbox billing, a 200-row immutable audit view and the pending `SYNTHETIC DEMO` deletion without submitting any mutation. A privacy-safe overview capture was retained as `screenshots/after/admin-overview-fresh-mfa-windows-150.png` with SHA-256 `173E7D5980C171504A05D7F5F09BEB986FA424FA9D2A25DF3BA225EA14A0A4CD`; the audit table was not captured because it displays production technical identifiers. Windows was then restored to DPR 1.25, and native Chrome page zoom was never changed.
- Windows Animation effects were then temporarily changed from on to off and restored to on. The authenticated Client dashboard's real Chrome media query changed `prefers-reduced-motion` from false to true and back to false. While reduced motion was active, the computed `body` transition shortened from 180 ms to 0.01 ms and the Platform sidebar from 220 ms to the intentional 120 ms bounded feedback; after restoration both Chrome's media query and the computed durations returned to their original values. This closes the live reduced-motion gate for the representative authenticated dashboard without claiming every route. Native Chrome page zoom is intentionally outside the QA scope by user instruction.
- Sites 74 Chrome smoke measured the live RU catalogue and demo profile at 1521/1521 pixels with no overflow, showed `Новый профиль`/`НОВЫЙ ПРОФИЛЬ`, and found no `Одобрен JURO` or mandatory-review copy. The Lawyer production profile after Worker `727eacbe-7fb6-4012-87a9-3e290edd525b` showed `Профиль опубликован` with no false approval language.

## Validation

- Website: build and 42/42 tests passed; type-check and lint passed. A fresh public-edge pass resolved `juro.uz`, `www`, `app`, `lawyer`, `admin` and `status` through Cloudflare, confirmed the expected host responses plus the `www` 308 canonical redirect, and received HTTP 200 from all 78 canonical RU/UZ/EN sitemap URLs. `robots.txt` points to the canonical sitemap; no sitemap URL or discovery file contains a forbidden lawyer-domain variant.
- Platform: the release passed type-check, lint, production-like build/artifact and performance budgets, rendered HTML 33/33, core 1066/1066 and Cloudflare 201/201 locally. GitHub CI run `32602970769` passed Website in 52 seconds and Platform in 7 minutes 50 seconds at evidence head `a1017397`; Worker 122 deployment and post-deploy Chrome/API smoke passed as bounded above.
- Isolated Admin: type-check and production dry-run passed. Worker `9cdbf8b1-ae02-4c32-9941-85d593064038` localizes the navigation and legal-corpus operations, replaces approval terminology with publication semantics, removes manual initial-approval controls and strengthens the operational overview hierarchy. Its live polished 1536-pixel Chrome capture is indexed below.
- Draft PR: [#64](https://github.com/MoozUpus/juro/pull/64).

## Remaining release evidence

- Screen-share source selection and forced reconnect. The Client dashboard, 19-route Lawyer suite and fresh-MFA Admin data screens passed the real 150% scale check; Windows was restored to its original 125% scale. The representative live reduced-motion cycle is also complete and Animation effects were restored.
- Replace the pre-incorporation legal preview with owner-approved operator identity, address and final RU/UZ legal editions before a commercial production claim. Current app registration policies remain deliberately versioned as draft and visibly disclose this status; no operator details are invented.
- Edge, Firefox, Safari/WebKit, physical iPhone/iPad/Android and native Chrome page zoom 125%/200% remain intentionally `NOT TESTED` by explicit user instruction.
