# JURO completion matrix — protected staging beta

> **Historical ledger.** Production was subsequently deployed on 2026-08-09.
> Current deployment, cleanup, backup/restore, separation, smoke and open-gate
> evidence is maintained in
> [`../../apps/platform/docs/ai-platform/PRODUCTION-CUTOVER-2026-08-09.md`](../../apps/platform/docs/ai-platform/PRODUCTION-CUTOVER-2026-08-09.md).

Last updated: 2026-08-09. This is an evidence ledger, not a release claim.
`VERIFIED_WORKING` means that the stated staging evidence and regression tests
exist; it does not authorize or describe a production change.

| Module | Route / API | Current status | Evidence | Existing implementation location | Missing behaviour | Security impact | Data migration required | Next action | Tests | Final status |
|---|---|---|---|---|---|---|---|---|---|---|
| Auth, OTP, sessions, workspace isolation | localized platform routes | VERIFIED_WORKING | Existing staging evidence through 0105 and authenticated Chrome Access smoke on 2026-08-07 | `lib/auth`, `_auth`, `app/api/auth` | Complete device and screen-reader matrix | Critical | No | Run final authenticated mobile matrix | Route/security suites | Preserved in staging |
| AI chat and structured response | `/api/platform/ai`, `/api/guest/ai`, `/:locale/:accountType/ai-chat` | VERIFIED_WORKING | 2026-08-07 authenticated browser: synthetic apostille answer and official-source panel rendered; a no-evidence request produced a no-charge clarification. Deep links now preserve the conversation across RU/UZ. | `lib/ai`, `app/api/platform/ai`, `_platform/AiLawyerClient.tsx`, `_platform/PlatformShell.tsx` | Broader legal evaluation remains a release gate | Critical | No | Execute final beta evaluation harness | `test`, `test:cloudflare`, focused browser smoke | Source-backed RU/UZ staging smoke passed |
| Query-scoped official sources | source panel, `/api/platform/legal-sources/health` | VERIFIED_WORKING | Advice card `https://advice.uz/ru/document/2620` rendered for a matching synthetic apostille query; generic jurisdiction/platform words now cannot make an unrelated source eligible and return an honest no-source clarification. | `lib/legal/direct-retrieval.ts`, migration `0106` | Lex coverage and independent legal review | Critical | No | Run source-coverage report and legal acceptance | 9 direct-source contracts + browser smoke | Staging verified; no corpus writes |
| Legacy legal corpus | legacy review/sync routes | DORMANT | Flags disable ingestion, RSS discovery and staff source API in staging | `wrangler.jsonc`, legacy legal tables/indexes | Decommission only under a separate retention plan | High | No | Keep rollback assets read-only and document any use | Flag/type regression | Dormant |
| Builder and document lifecycle | `/:locale/:accountType/document-builder` | VERIFIED_WORKING | Synthetic Builder → version → DOCX/PDF/ZIP staging evidence; the 2026-08-07 isolated `receipt-ru.docx` scanner pass again reached `analysis_safe` before AI dispatch. | `app/_document-builder`, `app/api/document-builder` | Complete a fresh upload-to-analysis run after provider recovery | Critical | No | Recover a configured AI provider, then repeat the isolated scanner-to-analysis flow | Builder/R2 suites | Builder and private R2 lifecycle remain verified in staging |
| Document analysis and compare | document-review / comparison routes | PARTIAL | Existing synthetic analysis accepted a revision and comparison rendered redline. Safe staging metadata shows a completed OpenAI fallback analysis on 2026-08-06, followed by sequential primary/fallback timeouts and a later `PROVIDER_UNAVAILABLE` state. On 2026-08-09 Worker `4e18a195-6c89-4d0e-a708-0c958e7e6429` was deployed: document jobs now allow one bounded attempt per provider by default, preserving time for the configured fallback. The scanner remains fail-closed and the content-free synthetic probe remains disabled by artifact policy. | `lib/document-analysis`, `app/_platform/DocumentReviewClient.tsx`, `lib/document-analysis/provider.ts` | Complete a fresh authenticated scanner → analysis → revision/compare smoke; 100-package beta remains owner-beta acceptance only | High | No | Use an authenticated staging account with a synthetic document, or an explicitly approved operational probe workflow; do not bypass the disabled-probe release guard | 129/129 platform regression, provider retry contract, staging deploy `4e18a195…` | Scanner, cleanup and safer fallback policy verified; fresh end-to-end AI completion remains unproven |
| Cases, plans, deadlines | case / action-plan routes | VERIFIED_WORKING | Saved AI answer created persisted steps, tasks and linked citations | `lib/platform/cases`, `app/_platform/CaseWorkspaceClient.tsx` | Complete touch/keyboard matrix | High | No | Repeat at mobile breakpoints | Lifecycle/action-plan suites | Staging browser smoke passed |
| Lawyer profile lifecycle and directory | `/api/platform/lawyer-profile`, `/api/platform/lawyer-profile/photo`, `/api/platform/lawyers`, `/api/public/lawyers`, `/api/platform/admin/lawyer-profiles/:profileId/lifecycle`, `admin.staging.juro.uz/lawyers` | PARTIAL | Staging migration `0110` is applied and independently restore-verified; platform Worker `3ae7c2d7-57fd-4263-8772-e65a27bf39a8` deploys immutable suspend/block/archive/restore evidence, localized notifications and fail-closed marketplace projection. On 2026-08-07, user-authorized Chrome QA created synthetic request `5b1494d8-ba9b-4228-8d98-9b6921f70067` for synthetic case `caac1563-5c0a-41d0-9b80-91747fd3c43c`; D1 confirms `conflict_check_pending` for approved staging fixture `22000000-0000-4000-8000-000000000004`. No case material was granted. | `lib/platform/lawyer-marketplace.ts`, `lib/platform/lawyer-profile-lifecycle-service.ts`, `lib/platform/lawyer-profile-notifications.ts`, `app/api/platform/lawyer-profile/**`, `app/api/platform/admin/lawyer-profiles/**`, `app/api/public/lawyers/**`, `apps/admin/src/worker.ts`, `_platform/LawyerDirectoryClient.tsx` | Bilateral conflict-check decision, explicit customer access grant, proposal and demo-payment lifecycle; protected staff browser smoke remains. | Critical | `0110` applied in staging only | Run read-only staff lifecycle and bilateral handoff checks through the restored Access session. | Full platform regression 129/129; Cloudflare suite; focused lifecycle suite; user-side synthetic request browser smoke | Restricted profiles cannot be edited, listed, viewed, or booked through public/request paths; production is unchanged. |
| Admin and demo payments | `admin.staging.juro.uz`, platform admin routes / payment demo routes | PARTIAL | Separate Access-protected admin Worker `d1358c9c-a62d-4579-8773-0f786de8eb1f` uses `https://app.juro.uz` as its staging return origin, host-only server session, platform-side fresh-MFA/role recheck and private service binding; it exposes auditable profile correction/review moderation plus status-filtered lifecycle controls. Platform Worker `4e18a195-6c89-4d0e-a708-0c958e7e6429` preserves demo labelling and sandbox-only payment behavior. | `apps/admin`, `app/[locale]/admin/console`, `app/api/platform/admin`, `lib/payments`, `_platform/BillingClient.tsx` | Complete authenticated browser handoff/moderation smoke; cancellation/failure smoke and fresh reviewer MFA | Critical | `0109` and `0110` already applied in staging; this UI release has no migration | Sign in through Cloudflare Access, then run read-only admin navigation and focused lifecycle checks before consented synthetic flow writes | Admin security/lifecycle 16/16, full platform regression 129/129, billing contract 11/11, staging build/artifact validation | Staging-only; not a production payment service |
| Public lawyer marketplace | `juro.uz/:locale/lawyers`, `/:locale/lawyers/:profileId` | PARTIAL | Website type-check, lint and production build pass; platform has public field-allowlisted list/detail endpoints | `apps/website/app/[locale]/lawyers/**`, `apps/platform/app/api/public/lawyers/**` | A public-site deployment and approved-profile detail SEO smoke need explicit production approval | Critical | No | Hold the validated change until the owner separately approves a public juro.uz deployment | Website build/artifact; marketplace lifecycle checks | Built and committed; not production-deployed |
| Cinematic UI and accessibility | shell, priority routes | PARTIAL | Authenticated Chrome desktop smoke has no console errors or horizontal overflow on core routes | `app/_platform/PlatformShell.tsx`, `app/globals.css` | Full mobile, reduced-motion, screen-reader and performance matrix | Medium | No | Run final QA and fix only evidenced issues | Rendered suite + browser smoke | In progress |

## Current non-release gates

- Independent, human legal-review evidence remains distinct from the owner's
  private staging-beta acceptance of 314 legal, 100 document and 30 comparison
  decisions.
- The scanner is working fail-closed in staging. A fresh synthetic
  scanner-to-Anthropic run on 2026-08-07 cleaned all mutable probe state yet
  returned only `PROVIDER_UNAVAILABLE` before a transport status; provider
  recovery and a fresh end-to-end analysis completion remain required before
  release.
- The public marketplace is built but still awaits its private Sites checkpoint
  and staging browser proof. Cloudflare Access to the platform staging session
  was restored on 2026-08-07; the separately deployed admin surface still
  awaits its final authenticated browser handoff proof.
- The production application release is prepared against isolated resources:
  `juro-production`, private production R2 buckets, 16 `production-*`
  queues/DLQs and four `production-*` Vectorize indexes. No production Worker
  deployment or D1 migration has yet occurred. Cloudflare secret values cannot
  be copied from staging; `OPENAI_API_KEY`, `ANTHROPIC_API_KEY` and
  `TURNSTILE_SECRET_KEY` must be set independently in production before the
  guarded release can proceed.
